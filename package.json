{
  "name": "my-yt-summarizer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "fs-extra": "^11.2.0"
  }
}
